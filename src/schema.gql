# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type UserSchema {
  id: String!
  age: Int!
  username: String!
  email: String!
  isActive: Boolean!
  tasks: [task!]!
}

type task {
  id: String!
  title: String!
  description: String!
  userId: String!
  priority: priorityType!
}

enum priorityType {
  low
  medium
  high
}

type Query {
  users(search: String, limit: Int!, offset: Int!): [UserSchema!]!
  userById(id: String!): UserSchema!
}

type Mutation {
  createUser(data: addUserInput!): UserSchema!
  updateUser(id: String!, data: addUserInput!): UserSchema!
  deleteUser(id: String!): UserSchema!
}

input addUserInput {
  username: String!
  email: String!
  age: Int!
}